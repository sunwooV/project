<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hm.F_P001_D004">
	<resultMap id="p0001Result01" type="F_P001_D004VO"> 
		<result property="memberId" column="memberId" />
		<result property="flea_code" column="flea_code" />
		<result property="together_yn" column="together_yn" />
		<result property="together_request_date" column="together_request_date" />
		<result property="together_approve_date" column="together_approve_date" />
	</resultMap>

	<select id="applyList" resultMap="p0001Result01" parameterType="java.util.Map"> 
      <![CDATA[
         select memberId, flea_code, together_yn, together_request_date, together_approve_date
         from togetherlist
      ]]>
	</select>

	<insert id="insertMember" parameterType="java.util.Map"> 
	<selectKey order="BEFORE" resultType="int" keyProperty="join_code">
		SELECT nvl2((max(join_code) + 1), (max(join_code) + 1), 1) FROM joinflea
	</selectKey>

         insert into joinflea(join_code, join_group, join_name, join_phonenumber, join_email, join_companynumber, join_product, join_sns, join_etc)
         values(#{join_code}, #{join_group}, #{join_name}, #{join_phonenumber}, #{join_email}, #{join_companynumber}, #{join_product}, #{join_sns}, #{join_etc})

	</insert>

	<select id="searchMod" resultMap="p0001Result01" parameterType="java.util.Map"> 
      <![CDATA[
         select id, pwd, name, email, joinDate
         from   t_member
      ]]>
		<where>
			<if test=" p_id != ''  and  p_id != null">
				id = #{p_id}
			</if>
		</where>
	</select>

	<update id="updateMember" parameterType="java.util.Map">
		update t_member
		set  pwd = #{pwd}
		   , name = #{name}
		   , email = #{email}
		where id = #{id}
	</update>
	
	

</mapper>